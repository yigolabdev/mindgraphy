name: Deploy to Vercel (Recommended for SSR)

# ⚠️ 중요: 이 프로젝트는 동적 라우트를 사용하므로 정적 빌드(S3) 대신
# 서버사이드 렌더링을 지원하는 플랫폼(Vercel, Netlify)을 권장합니다.

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  # 이 워크플로우는 비활성화되어 있습니다.
  # Vercel을 사용하는 경우 Vercel Git Integration을 활성화하세요:
  # https://vercel.com/docs/deployments/git
  
  placeholder:
    runs-on: ubuntu-latest
    if: false # 비활성화
    steps:
      - name: Info
        run: |
          echo "이 워크플로우는 비활성화되어 있습니다."
          echo "Vercel 또는 Netlify Git Integration을 사용하여 자동 배포하세요."
